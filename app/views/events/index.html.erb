<!-- Page d'accueil affichant tous les événements -->
<div class="container">
  <div class="jumbotron mt-4">
    <h1 class="display-4">Bienvenue sur EventBrite</h1>
    <p class="lead">Trouvez et participez à des événements passionnants près de chez vous.</p>
    <hr class="my-4">
    <p>Pour commencer, créez votre propre événement ou parcourez les événements disponibles.</p>
  </div>

  <!-- Liste des événements -->
  <div class="row">
    <% @events.each do |event| %>
      <div class="col-md-4">
        <div class="card mb-4 shadow-sm h-100"> <!-- Ajout de la classe h-100 -->
          <div class="card-body">
            <h5 class="card-title"><%= event.title %></h5>
            <p class="card-text"><%= event.description %></p>
            <div class="d-flex justify-content-between align-items-center">
              <div class="btn-group">
                <%= link_to 'Voir plus', event_path(event), class: 'btn btn-sm btn-outline-secondary' %>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
